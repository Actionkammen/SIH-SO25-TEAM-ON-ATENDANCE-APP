server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/attendai_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: Aanand@2006
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: create-drop   # clears on restart
    show-sql: true
    generate-ddl: true

jwt:
  secret: mysecretdonttell
  expiration: 86400000